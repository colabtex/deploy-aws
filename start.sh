#!/bin/bash

<<README
    The sole purpose of this file is to serve as a clean entry point for all
    the other scripts to be called (though technically build.sh is the one that
    actually calls those other scripts)
README

bash build.sh